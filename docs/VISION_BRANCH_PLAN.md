# Vision branch

`features/vision` is the canonical development branch for the vision-to-CAD pipeline.

Base: `feature/visual-fluent-v2`.

The branch preserves the working visual verification flow and selectively incorporates the structured vision contract, deterministic validation, and model/provider improvements from `agent-v2-vision`.
