# Что нужно сделать тебе

Ветка: **`dev`**

---

## Статус по твоим логам (`responce.txt`)

| Проверка | Результат |
|----------|-----------|
| COM + КОМПАС-23 | OK (`visible=True`) |
| Python | 3.14.0 |
| LLM Groq | Ошибка: модель `llama-3.3-70b-versatile` недоступна на твоём тарифе |

---

## Сделай сейчас

### 1. Безопасность ключа (обязательно)

В git попал API-ключ в `.env.example`.  
На https://console.groq.com → **удали/revoke старый ключ** и создай **новый**.

### 2. Обнови `.env` в корне проекта

```env
LLM_PROVIDER=groq
GROQ_API_KEY=новый_ключ_сюда
LLM_MODEL=llama-3.1-8b-instant
LLM_TEMPERATURE=0.2
```

Именно `llama-3.1-8b-instant` — обычно доступен на free.

### 3. Подтяни правки и проверь агента

```powershell
git pull origin dev

python -m agent.runner "Втулка: наружный диаметр 40, внутренний 20, длина 50"
```

Ожидается блок Python-кода в консоли.

---

## Дальше

Когда агент выдаст код — пришли его сюда (или снова в файл в репо).  
Проверим и только потом запустим построение в КОМПАСе.
