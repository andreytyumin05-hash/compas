# UX + логика

Telegram теперь пишет **по-человечески**: что за деталь, размеры, шаги построения — без `shape=` и сырого JSON.

После сборки — коротко «Готово», без простыни кода.

```powershell
git pull origin agent-v2-vision
python -m bot
```

Сложная геометрия по-прежнему ограничена core (ступени/карманы/fillet). Если снова путает — правь описание текстом после «Неверно».

