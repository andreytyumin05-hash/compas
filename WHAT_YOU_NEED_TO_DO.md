# Исправление сборки

Проблема: vision читал чертёж, а **шаблон «крышка»** строил параллелепипед.

Сейчас:
- многоступенчатые / пробка / вал / несколько Ø → **только LLM**, шаблон не перехватывает
- шаблон проходит feature-check или отбрасывается
- промпт: ступени = circle+extrude, карманы = cut

```powershell
git pull origin agent-v2-vision
python -m bot
```

Проверка `.env`: `GEMINI_API_KEY=...` (иначе vision молчит).
